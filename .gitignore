# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.env
.env.local
.env.*.local
.env.development
.env.production
backend/.env
backend/.env.*

memory.json

# Editor directories and files
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust build artifacts
backend/target/
**/target/
*.pdb
*.ooo

# Cargo lock for workspace (keep for reproducible builds)
# backend/Cargo.lock

pnpm-lock.yaml
yarn.lock
bun.lockb

# Windows reserved names
nul

# Portal (now separate repo: Aster-Portal)
portal/

# Packages (development)
packages/

# Old backend backup (now separate repo)
backend.old/

# SSL certs
cert.pem
key.pem

# Workspace setup script
setup_workspace.sh
